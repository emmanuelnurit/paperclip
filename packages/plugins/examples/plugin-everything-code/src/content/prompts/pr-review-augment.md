# PR review — Everything Code augmentation

Before posting a PR review:

1. Run `everything_code__code_review` on the diff for a structured baseline.
2. Run `everything_code__security_review` on touched config files.
3. Run `everything_code__quality_gate` if you have project-level scope.
4. Combine the three into a single concise review comment with explicit follow-ups.
